---
import { t, type Locale } from '../i18n';
import { getLocaleFromPath } from '../lib/routes';
import { pageUrl } from '../lib/site-map';

interface Props {
  locale?: Locale;
}

const { locale: localeProp } = Astro.props;
const locale = localeProp ?? getLocaleFromPath(Astro.url.pathname);
const m = t(locale);
const f = m.footer;
const co = m.company;
const year = new Date().getFullYear();
const companyPath = pageUrl(locale, locale === 'vi' ? 'gioi-thieu' : 'company');
const contactPath = locale === 'vi' ? '/vi/lien-he' : '/en/contact';
const link =
  locale === 'vi'
    ? {
        aiAutomation: 'Tự động hóa AI',
        healthcare: 'Ngành y tế',
        retail: 'Bán lẻ',
        aiTransform: 'Chuyển đổi AI',
        company: 'Giới thiệu',
        insights: 'Kiến thức',
        careers: 'Tuyển dụng',
      }
    : {
        aiAutomation: 'AI Automation',
        healthcare: 'Healthcare',
        retail: 'Retail',
        aiTransform: 'AI Transformation',
        company: 'Company',
        insights: 'Insights',
        careers: 'Careers',
      };
---

<footer class="site-footer">
  <div class="container footer-grid">
    <div class="footer-brand">
      <p class="footer-name">{m.meta.siteName}</p>
      <p class="footer-tagline">{f.tagline}</p>
      <p class="footer-locations">{f.locations.join(' · ')}</p>
      <p class="footer-contact">
        <a href={`tel:${m.contact.phoneTel}`}>{m.contact.phoneDisplay}</a>
        ·
        <a href={`mailto:${m.contact.emailAddress}`}>{m.contact.emailAddress}</a>
      </p>
      <div class="footer-legal">
        <p>{co.legalName}</p>
        <p>{co.addressShort}</p>
        <p>{co.taxIdLabel}: {co.taxId}</p>
      </div>
    </div>

    <div class="footer-col">
      <p class="footer-col-title">{f.columns.products}</p>
      <a href={pageUrl(locale, locale === 'vi' ? 'san-pham/novixa' : 'products/novixa')}>Novixa</a>
      <a href={pageUrl(locale, locale === 'vi' ? 'san-pham/ai-automation' : 'products/ai-automation')}>{link.aiAutomation}</a>
      <a href={pageUrl(locale, locale === 'vi' ? 'san-pham/ai-agent' : 'products/ai-agent')}>AI Agent</a>
    </div>

    <div class="footer-col">
      <p class="footer-col-title">{f.columns.solutions}</p>
      <a href={pageUrl(locale, locale === 'vi' ? 'giai-phap/healthcare' : 'solutions/healthcare')}>{link.healthcare}</a>
      <a href={pageUrl(locale, locale === 'vi' ? 'giai-phap/retail' : 'solutions/retail')}>{link.retail}</a>
      <a href={pageUrl(locale, locale === 'vi' ? 'giai-phap/ai-transformation' : 'solutions/ai-transformation')}>{link.aiTransform}</a>
    </div>

    <div class="footer-col">
      <p class="footer-col-title">{f.columns.company}</p>
      <a href={companyPath}>{link.company}</a>
      <a href={pageUrl(locale, locale === 'vi' ? 'blog' : 'insights')}>{link.insights}</a>
      <a href={pageUrl(locale, locale === 'vi' ? 'tuyen-dung' : 'careers')}>{link.careers}</a>
      <a href={contactPath}>{m.nav.contact}</a>
    </div>
  </div>

  <div class="container footer-bottom">
    <p>© {year} {f.rights} · {f.since}</p>
    <p class="footer-social">
      <span>{f.social.linkedin}</span>
      <span>{f.social.facebook}</span>
      <span>{f.social.github}</span>
    </p>
  </div>
</footer>

<style>
  .site-footer {
    background: var(--color-text);
    color: #cbd5e1;
    padding: 3rem 0 1.5rem;
    margin-top: 2rem;
    font-size: 0.9rem;
  }

  .footer-grid {
    display: grid;
    gap: 2rem;
    grid-template-columns: 1.4fr repeat(3, 1fr);
    margin-bottom: 2rem;
  }

  @media (max-width: 768px) {
    .footer-grid {
      grid-template-columns: 1fr 1fr;
    }
    .footer-brand {
      grid-column: 1 / -1;
    }
  }

  .footer-name {
    margin: 0 0 0.25rem;
    font-size: 1.15rem;
    font-weight: 700;
    color: #fff;
  }

  .footer-tagline,
  .footer-locations {
    margin: 0.2rem 0;
    color: #94a3b8;
  }

  .footer-tagline {
    color: #cbd5e1;
  }

  .footer-contact {
    margin-top: 0.75rem;
  }

  .footer-contact a {
    color: #5eead4;
  }

  .footer-legal {
    margin-top: 1.25rem;
    padding-top: 1rem;
    border-top: 1px solid #334155;
    font-size: 0.78rem;
    line-height: 1.55;
    color: #64748b;
  }

  .footer-legal p {
    margin: 0.2rem 0;
  }

  .footer-col-title {
    margin: 0 0 0.65rem;
    font-weight: 700;
    color: #fff;
    font-size: 0.82rem;
    text-transform: uppercase;
    letter-spacing: 0.04em;
  }

  .footer-col {
    display: flex;
    flex-direction: column;
    gap: 0.35rem;
  }

  .footer-col a {
    color: #94a3b8;
    text-decoration: none;
  }

  .footer-col a:hover {
    color: #5eead4;
  }

  .footer-bottom {
    display: flex;
    flex-wrap: wrap;
    justify-content: space-between;
    gap: 0.5rem;
    padding-top: 1.25rem;
    border-top: 1px solid #334155;
    font-size: 0.82rem;
    color: #64748b;
  }

  .footer-social {
    display: flex;
    gap: 1rem;
    margin: 0;
  }
</style>
